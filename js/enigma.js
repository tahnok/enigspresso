// Generated by CoffeeScript 1.6.3
(function() {
  var Enigma, a, arrayToString, caesarCipher, decode, encode, letterToNumber, numberToLetter, stringToArray;

  letterToNumber = function(letter) {
    var temp;
    temp = letter.toLowerCase().charCodeAt(0);
    if (temp === 32) {
      temp;
    } else if (temp < 97 || temp > 122) {
      throw "must be a letter between a and z";
    }
    return temp - 96;
  };

  numberToLetter = function(number) {
    var offset;
    offset = number === 32 ? 0 : 96;
    return String.fromCharCode(number + offset);
  };

  stringToArray = function(string) {
    var char, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = string.length; _i < _len; _i++) {
      char = string[_i];
      _results.push(letterToNumber(char));
    }
    return _results;
  };

  arrayToString = function(array) {
    return array.map(numberToLetter).join("");
  };

  caesarCipher = function(message, key) {
    var a, b;
    a = stringToArray(message);
    b = a.map(function(i) {
      return i + key;
    });
    return arrayToString(b);
  };

  encode = function(message, key) {
    return caesarCipher(message, key);
  };

  decode = function(message, key) {
    return caesarCipher(message, -key);
  };

  console.log(decode(encode("attack", 4), 4));

  Enigma = (function() {
    function Enigma(state) {
      this.state = state;
    }

    Enigma.prototype.encodeLetter = function(letter) {
      return alert(this.name + (" moved " + meters + "m."));
    };

    return Enigma;

  })();

  a = new Enigma("test");

}).call(this);
